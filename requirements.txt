# Requires Python >= 3.11

# App requirements
# Security: All dependencies pinned to specific versions to prevent supply chain attacks
psutil==5.9.8
requests==2.31.0
websocket-client==1.7.0
websockets==12.0
Pillow>=11.0.0
pystray>=0.19.5

# Build requirements
pyinstaller>=6.18.0